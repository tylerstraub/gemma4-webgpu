[project]
name = "gemma4-webgpu-reference"
version = "0.1.0"
description = "HF Transformers forward-hook reference tensors for gemma4-webgpu correctness verification"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
    "torch>=2.11.0",
    "transformers>=5.5.4",
]
